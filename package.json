{
  "scripts": {
    "build": "run-s build:languages build:extension",
    "build:extension": "web-ext build --overwrite-dest -i generate-language-json.js -i package.json -i package-lock.json",
    "build:languages": "node generate-language-json.js"
  },
  "dependencies": {
    "axios": "^0.26.1",
    "js-yaml": "^4.1.0",
    "npm-run-all": "^4.1.5",
    "web-ext": "^6.8.0"
  }
}
